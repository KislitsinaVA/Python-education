# Первый вариант с **
x = float(input("Введите действительное положительное число: "))
y = int(input("Введите целое отрицательное число: "))


def my_func(x, y):
    if x > 0 and y < 0:
        return x ** y
    else:
        print("Ошибка ввода: x - действительное положительное число,\n у - целое отрицательное")


print(my_func(x, y))
# 2 Вариант с возведением в отрицательную степень через деление. Но идей по циклу нет.

def my_func(x, y):

    dividend = 1/(x ** abs(y))
    return f" x в степени у = {dividend}"


print(my_func(float(input("Введите действительное положительное число х = ")),
              int(input("Введите отрицательное целое число у = "))))